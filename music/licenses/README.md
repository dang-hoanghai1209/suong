# Music licenses

Store one local license or provenance record per production track. Track
entries in `music/library.json` must reference an existing file in this
directory. Production audio under `music/tracks/` is ignored by Git.
